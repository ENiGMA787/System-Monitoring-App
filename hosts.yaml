hosts:
  linux1:
    type: linux
    hostname: IP
    auth_method: password
    username: username
    password: password
  linux2:
    type: linux
    hostname: IP
    auth_method: password
    username: username
    password: password
  # windows1:
  #   type: windows
  #   hostname: IP
  #   username: username
  #   password: password
  # linux1:
  #   type: linux
  #   hostname: IP
  #   auth_method: private_key
  #   username: username
  #   key_path: /path/to/private/key
